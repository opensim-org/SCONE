#include "stdafx.h"
#include "Measure.h"

namespace scone
{
	namespace cs
	{

		Measure::Measure( const PropNode& props, opt::ParamSet& par, sim::Model& model ) : Controller( props, par, model )
		{
		}
	}
}
